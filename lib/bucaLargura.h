#ifndef __BUSCALARGURA_H
#define __BUSCALARGURA_H

#include <stdio.h>
#include <stdlib.h>

#include "structVertice.h"

#define TAMANHO_MAX_MATRIZ 25

// int buscaLargura(int grafo[5][5], int idVerticeInicial, int idVerticeFinal) {
//     int caminhoIds[5] = {0};
// }

// int *buscaFilhos(Svertice *vertice, int grafo[5][5]) {
//     int *filhos, intAux = 0;
//     int linha = vertice->idVertice; // A linha da matriz de adjacência que representa o nó emergente

//     for (int coluna = 0; coluna < 5; coluna++) {
//         if (grafo[linha][coluna] != 0) {
//             filhos[intAux] = coluna;
//         }
//         else {
//             filhos[intAux] = -1;
//         }
//         intAux++;
//     }

//     return filhos;
// }


#endif